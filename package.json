{
  "name": "rym-ratings-scale-10",
  "version": "1.0.0",
  "description": "Chrome extension to convert RateYourMusic ratings from 5-point to 10-point scale",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && jest",
    "format:write": "prettier . --write",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/jsdom": "^27.0.0",
    "@types/node": "^20.11.30",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "prettier": "^3.7.4",
    "ts-jest": "^29.4.6",
    "typescript": "^5.4.3"
  },
  "overrides": {
    "test-exclude": "^7.0.1"
  }
}
